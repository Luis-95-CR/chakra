// Central place for non-technical, business-facing settings.
// Edit these values to rebrand the site.

export const siteConfig = {
  /** Business name shown in the header and page title. */
  name: "La Chakra",
  /** Short label shown under the name (e.g. business type). */
  kicker: "Granja porcina · Desde 2015",
  /** Short tagline shown on the catalog page. */
  tagline: "Lista de precios",
  /** Hero headline + subheadline for the landing page. */
  heroTitle: "Carne fresca, directo de la finca a su mesa",
  heroSubtitle:
    "Res, cerdo y pollo de calidad, criados con cuidado en La Chakra. Consulte nuestra lista de precios actualizada.",
  /** ISO 4217 currency code used to format every price. */
  currency: "CRC",
  /** Locale used for number/date formatting. */
  locale: "es-CR",
} as const;
